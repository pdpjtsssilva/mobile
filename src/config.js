// Configuração de endpoints para o servidor L'europe no Render
const BASE_URL = 'https://backend1-33rl.onrender.com';

export const API_URL = `${BASE_URL}/api`;
export const SOCKET_URL = BASE_URL;

export default {
    API_URL,
    SOCKET_URL,
};